package Interfaz;

import java.awt.Image;

import javax.swing.ImageIcon;
import javax.swing.JButton;

public class CargarImagenes {
	
	static void imagenButton1(JButton button) {
	    ImageIcon originalIcon = new ImageIcon(MainForm.class.getResource("/Imagenes/d5aaffcecf2e3e9a6ca023ba5404b68f.jpeg"));
	    Image originalImage = originalIcon.getImage();
	    int buttonWidth = button.getWidth();
	    int buttonHeight = button.getHeight();
	    Image scaledImage = originalImage.getScaledInstance(buttonWidth, buttonHeight, Image.SCALE_SMOOTH);
	    button.setIcon(new ImageIcon(scaledImage));
	}
	
	static void imagenButton2(JButton button) {
	    ImageIcon originalIcon = new ImageIcon(MainForm.class.getResource("/Imagenes/d5aaffcecf2e3e9a6ca023ba5404b68f.jpeg"));
	    Image originalImage = originalIcon.getImage();
	    int buttonWidth = button.getWidth();
	    int buttonHeight = button.getHeight();
	    Image scaledImage = originalImage.getScaledInstance(buttonWidth, buttonHeight, Image.SCALE_SMOOTH);
	    button.setIcon(new ImageIcon(scaledImage));
	}
	
	static void imagenButton3(JButton button) {
	    ImageIcon originalIcon = new ImageIcon(MainForm.class.getResource("/Imagenes/d5aaffcecf2e3e9a6ca023ba5404b68f.jpeg"));
	    Image originalImage = originalIcon.getImage();
	    int buttonWidth = button.getWidth();
	    int buttonHeight = button.getHeight();
	    Image scaledImage = originalImage.getScaledInstance(buttonWidth, buttonHeight, Image.SCALE_SMOOTH);
	    button.setIcon(new ImageIcon(scaledImage));
	}
	
	static void imagenButton4(JButton button) {
	    ImageIcon originalIcon = new ImageIcon(MainForm.class.getResource("/Imagenes/d5aaffcecf2e3e9a6ca023ba5404b68f.jpeg"));
	    Image originalImage = originalIcon.getImage();
	    int buttonWidth = button.getWidth();
	    int buttonHeight = button.getHeight();
	    Image scaledImage = originalImage.getScaledInstance(buttonWidth, buttonHeight, Image.SCALE_SMOOTH);
	    button.setIcon(new ImageIcon(scaledImage));
	}
	
	static void imagenButton5(JButton button) {
	    ImageIcon originalIcon = new ImageIcon(MainForm.class.getResource("/Imagenes/d5aaffcecf2e3e9a6ca023ba5404b68f.jpeg"));
	    Image originalImage = originalIcon.getImage();
	    int buttonWidth = button.getWidth();
	    int buttonHeight = button.getHeight();
	    Image scaledImage = originalImage.getScaledInstance(buttonWidth, buttonHeight, Image.SCALE_SMOOTH);
	    button.setIcon(new ImageIcon(scaledImage));
	}
	
	static void imagenButton6(JButton button) {
	    ImageIcon originalIcon = new ImageIcon(MainForm.class.getResource("/Imagenes/d5aaffcecf2e3e9a6ca023ba5404b68f.jpeg"));
	    Image originalImage = originalIcon.getImage();
	    int buttonWidth = button.getWidth();
	    int buttonHeight = button.getHeight();
	    Image scaledImage = originalImage.getScaledInstance(buttonWidth, buttonHeight, Image.SCALE_SMOOTH);
	    button.setIcon(new ImageIcon(scaledImage));
	}
	
	static void imagenButton7(JButton button) {
	    ImageIcon originalIcon = new ImageIcon(MainForm.class.getResource("/Imagenes/d5aaffcecf2e3e9a6ca023ba5404b68f.jpeg"));
	    Image originalImage = originalIcon.getImage();
	    int buttonWidth = button.getWidth();
	    int buttonHeight = button.getHeight();
	    Image scaledImage = originalImage.getScaledInstance(buttonWidth, buttonHeight, Image.SCALE_SMOOTH);
	    button.setIcon(new ImageIcon(scaledImage));
	}
	
	static void imagenButton8(JButton button) {
	    ImageIcon originalIcon = new ImageIcon(MainForm.class.getResource("/Imagenes/d5aaffcecf2e3e9a6ca023ba5404b68f.jpeg"));
	    Image originalImage = originalIcon.getImage();
	    int buttonWidth = button.getWidth();
	    int buttonHeight = button.getHeight();
	    Image scaledImage = originalImage.getScaledInstance(buttonWidth, buttonHeight, Image.SCALE_SMOOTH);
	    button.setIcon(new ImageIcon(scaledImage));
	}
	
	static void imagenButton9(JButton button) {
	    ImageIcon originalIcon = new ImageIcon(MainForm.class.getResource("/Imagenes/d5aaffcecf2e3e9a6ca023ba5404b68f.jpeg"));
	    Image originalImage = originalIcon.getImage();
	    int buttonWidth = button.getWidth();
	    int buttonHeight = button.getHeight();
	    Image scaledImage = originalImage.getScaledInstance(buttonWidth, buttonHeight, Image.SCALE_SMOOTH);
	    button.setIcon(new ImageIcon(scaledImage));
	}
}
